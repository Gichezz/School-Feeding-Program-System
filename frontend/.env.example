# Frontend environment variables
# Copy to .env for local overrides (optional in Phase 1).

# Base URL for the Express API.
# Leave empty / use /api when relying on the Vite proxy in development.
# If running backend separately, use: http://localhost:5000/api
VITE_API_BASE_URL=/api
