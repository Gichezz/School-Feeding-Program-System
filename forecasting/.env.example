# Forecasting service environment variables
# Copy to .env when the forecasting phase begins.

FLASK_ENV=development
FLASK_RUN_PORT=5000

# Optional: connection string if the Flask service reads historical
# data directly from PostgreSQL in a later phase.
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/school_feeding
